<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kanbanboard">

	<select id="findCardAll" resultType="cardvo">
		<![CDATA[select no, title, description, status
				from card 
		        order by no desc
		]]>
	</select>
	
	<select id="findTaskAll" resultType="taskvo" parameterType="long">
		<![CDATA[select no, name, done, card_no
				from task where card_no = #{no} 
		        order by no desc
		]]>
	</select>
	
	<insert id="insert" parameterType="taskvo">
		<![CDATA[
		insert
		  into task
		values (null, #{name}, #{done}, #{cardNo})
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>		
		</selectKey>
	</insert>
	
	<delete id="delete" parameterType="long">
		<![CDATA[
		delete from task where no=#{no}
		]]>
	</delete>
	
	<update id="update" parameterType="taskvo">
	<![CDATA[
		update  task set done=#{done} where no=#{no}
		]]>
	</update>
</mapper>
